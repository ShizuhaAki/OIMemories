#include <bits/stdc++.h>
using namespace std;
int T;
int vis[101];
int main()
{
	cin >> T;
	while (T--)
	{
		memset(vis, 0, sizeof(vis));                                 
		string s, ans;
		cin >> s;
		sort(s.begin(), s.end());
		cerr << s << endl;
		int i, j;
		bool flag;
		for (i = 0; i < s.length() ; i++)
		{
			for (int j = i; j < s.allocator_t)
		}
		if (!flag)puts("No answer");
		else puts(ans.c_str());
	}
	return 0;
}

